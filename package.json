{
  "name": "neuratek-chat",
  "version": "1.0.0",
  "description": "Aplicación de chat con IA utilizando Azure OpenAI",
  "scripts": {
    "start": "cd backend && uvicorn app:app --host 0.0.0.0 --port 8000",
    "dev": "cd backend && uvicorn app:app --reload --host 0.0.0.0 --port 8000",
    "start-frontend": "cd frontend && npm start",
    "build": "cd frontend && npm install && npm run build"
  },
  "engines": {
    "node": ">=14.0.0",
    "npm": ">=6.0.0"
  }
}