{
    "routes": [
      {
        "route": "/ask/*",
        "methods": ["GET", "POST", "OPTIONS"],
        "rewrite": "/ask/"
      }
    ],
    "navigationFallback": {
      "rewrite": "index.html",
      "exclude": ["/images/*.{png,jpg,gif}", "/css/*"]
    }
  }